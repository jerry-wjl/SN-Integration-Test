---
- name: Sentinel A - Automated Incident Listener
  hosts: localhost
  gather_facts: false
  collections:
    - servicenow.itsm

  tasks:
    - name: 1. Poll ServiceNow for NEW VM Provisioning Tickets
      servicenow.itsm.incident_info:
        sysparm_query: "short_descriptionLIKEVM Provision^state=1"
      register: incident_result

    # --- 逻辑 B：如果没有新工单，直接优雅结束当前 Playbook ---
    - name: Stop if no new tickets found
      ansible.builtin.meta: end_play
      when: incident_result.records | length == 0

    # --- 逻辑 A：发现工单，开始处理 ---
    - name: 2. Capture and Acknowledge the Ticket (Move to Active)
      servicenow.itsm.incident:
        number: "{{ incident_result.records[0].number }}"
        state: 2  # Active
        other:
          assigned_to: "{{ lookup('env', 'SN_USERNAME') }}"
          work_notes: "AAP Schedule: Ticket captured and processing started."

    - name: 3. Get attachment metadata via REST
      ansible.builtin.uri:
        url: "{{ lookup('env', 'SN_HOST') }}/api/now/table/sys_attachment?sysparm_query=table_sys_id={{ incident_result.records[0].sys_id }}"
        method: GET
        user: "{{ lookup('env', 'SN_USERNAME') }}"
        password: "{{ lookup('env', 'SN_PASSWORD') }}"
        force_basic_auth: yes
        status_code: 200
      register: api_attach_info

    - name: 4. Download Excel Attachment
      servicenow.itsm.attachment_info:
        instance:
          host: "{{ lookup('env', 'SN_HOST') }}"
          username: "{{ lookup('env', 'SN_USERNAME') }}"
          password: "{{ lookup('env', 'SN_PASSWORD') }}"
        sys_id: "{{ api_attach_info.json.result[0].sys_id }}"
        dest: "/tmp/Provision_Request_Form_v1.xlsx"

    - name: 5. Parse Excel via Python
      ansible.builtin.shell: |
        python3 << EOF
        import pandas as pd
        import json
        import re
        try:
            df = pd.read_excel('/tmp/Provision_Request_Form_v1.xlsx', header=None)
            def extract(key, is_num=False):
                mask = df.apply(lambda row: row.astype(str).str.contains(key, case=False, regex=False, na=False).any(), axis=1)
                if not df[mask].empty:
                    vals = df[mask].iloc[0].dropna().values
                    if len(vals) > 1:
                        raw = str(vals[-1]).strip()
                        if is_num:
                            match = re.search(r'(\d+)', raw)
                            return int(match.group(1)) if match else 0
                        return raw
                return 0 if is_num else ""

            data = {
                "ticket_context": {
                    "number": "{{ incident_result.records[0].number }}",
                    "sys_id": "{{ incident_result.records[0].sys_id }}"
                },
                "server_info": {
                    "hostname": extract("Server Description"),
                    "os": extract("OS Version")
                },
                "vm_specs": {
                    "cpu": extract("#CPU Cores", is_num=True),
                    "ram_gb": extract("Memory", is_num=True)
                },
                "storage": {
                    "total_gb": extract("Total Disk Size", is_num=True)
                }
            }
            print(json.dumps(data))
        except Exception as e:
            print(json.dumps({"error": str(e)}))
        EOF
      register: python_out

    - name: 6. Handover Variables to Workflow
      ansible.builtin.set_stats:
        data:
          provision_data: "{{ python_out.stdout | from_json }}"
          has_work: true

    - name: 7. Log Success to ServiceNow
      servicenow.itsm.incident:
        sys_id: "{{ incident_result.records[0].sys_id }}"
        other:
          work_notes: "AAP successfully extracted variables. Ready for Provisioning."
